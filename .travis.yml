os: osx
language: node_js
node_js: "8"
env:
  global:
    - _FORCE_LOGS=1
matrix:
  include:
    - osx_image: xcode9.4
    - osx_image: xcode10.2
script:
  - npm run test
  - npm run e2e-test
after_success:
  - if [ -n "$COVERALLS" ]; then _FORCE_LOGS=0 npm run coverage; fi
